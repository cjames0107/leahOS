tee - copy input to output and to files

SYNOPSIS
    tee [-a] FILE...

DESCRIPTION
      -a   append rather than truncate

    The one command whose whole purpose is that a pipeline is a line: it lets
    something in the middle of one be looked at without being taken out of
    it.

SEE ALSO
    cat, sh
