wc - count lines, words and bytes

SYNOPSIS
    wc [-l] [-w] [-c] [FILE...]

DESCRIPTION
    With no options, prints all three. With any, prints only those asked for.

    A word is a run of anything that is not whitespace, which is the same
    definition every other wc uses and is wrong for the same edge cases.

SEE ALSO
    grep, sort
