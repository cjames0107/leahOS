tar - bundle files into one file

SYNOPSIS
    tar -c|-x|-t -f ARCHIVE [FILE...]

DESCRIPTION
      -c   create        -x   extract        -t   list
      -f   which archive

    The plain format, without compression. gunzip handles the other half.

SEE ALSO
    gunzip, cp
