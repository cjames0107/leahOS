uptime - how long the machine has been up

SYNOPSIS
    uptime

DESCRIPTION
    Prints the time of day and how long since boot.

    No load average. A load average is a decaying count of runnable tasks
    kept by the scheduler, and this one does not keep it - a made-up number
    where people read a real one would be worse than a gap.

SEE ALSO
    ps, date
