diff - what changed between two files

SYNOPSIS
    diff OLD NEW

DESCRIPTION
    Prints the lines that differ, in the usual < and > form.

    Line by line, without the longest-common-subsequence work a real diff
    does, so a line inserted near the top makes everything after it look
    changed.

SEE ALSO
    cmp, grep
