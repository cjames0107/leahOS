mv - move or rename

SYNOPSIS
    mv SOURCE DESTINATION

DESCRIPTION
    Renames a file, or moves it into a directory.

    Within one filesystem this is a change of directory entry and nothing
    else - the inode does not move, so the file keeps its identity, its
    timestamps and any other names it has.

SEE ALSO
    cp, rm
