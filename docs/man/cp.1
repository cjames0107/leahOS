cp - copy files

SYNOPSIS
    cp SOURCE DESTINATION

DESCRIPTION
    Copies a file. If the destination is a directory, the copy goes inside it
    under the source's own name.

    The mode is copied with the contents, so a program stays a program.

SEE ALSO
    mv, rm, ln
