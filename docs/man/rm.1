rm - remove files

SYNOPSIS
    rm [-r] [-f] NAME...

DESCRIPTION
      -r   remove a directory and everything in it
      -f   say nothing about names that are not there

    Removing a symbolic link removes the link, never what it points at.

SEE ALSO
    rmdir, mkdir, ln
