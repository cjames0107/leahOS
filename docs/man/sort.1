sort - put lines in order

SYNOPSIS
    sort [-r] [-n] [-u] [FILE...]

DESCRIPTION
      -r   reverse
      -n   compare as numbers rather than as text
      -u   drop duplicates, as uniq would afterwards

SEE ALSO
    uniq, grep, wc
