pwd - print the working directory

SYNOPSIS
    pwd

DESCRIPTION
    Writes the current directory. The shell also keeps it in $PWD, which is
    the same answer without a process.

SEE ALSO
    cd, sh
